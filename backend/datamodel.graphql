type User {
  id: ID! @id @unique
  name: String!
  email: String!
  password: String!
}

type Language {
  id: ID! @id @unique
  name: String!
}

enum FeatureType {
  CLASS
  FOLKAL
}

type Feature {
  id: ID! @id @unique
  name: String!
  description: String!
  comesFrom: FeatureType!
}

type Folk {
  id: ID! @id @unique
  name: String!
  strMod: Int!
  conMod: Int!
  dexMod: Int!
  chaMod: Int!
  intMod: Int!
  wisMod: Int!
  languages: [Language!]!
  features: [Feature]!
}
