import styled from 'styled-components';
import { useMutation, gql } from '@apollo/client';

const CreateCharacter = () => {
  return <div>Character Creation Goes Here</div>;
};

export default CreateCharacter;
