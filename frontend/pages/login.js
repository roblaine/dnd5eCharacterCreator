import Button from '../components/Button';

class Login extends React.Component {
  render() {
    return (
      <div>
        Login
        <Button key="login-back-button" path="/" text="Back" />
      </div>
    );
  }
}

export default Login;
